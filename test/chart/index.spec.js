import {expect} from "chai";

import {Chart, DataObject, OptionObject, Buffer} from "../../src/index";

describe("basic", ()=>{
    it("should just init and redraw", ()=>{
        expect(true).to.equal(true);
    });
});
